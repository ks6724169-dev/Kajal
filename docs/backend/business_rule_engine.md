# Business Rule Engine

## Purpose
Decouples complex approval matrices, workflow conditions, and feature flags from standard service logic.
Provides an extensible `BusinessRule` interface that evaluates a given `RuleContext`.
