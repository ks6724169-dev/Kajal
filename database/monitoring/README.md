# DATABASE TELEMETRY & MONITORING
This directory houses telemetry collectors and views monitoring database health, disk allocations, connection volumes, and slow-running queries.

## Core Telemetry Components
- **db_health_metrics**: Measures CPU, Memory, Disk and Connection volumes.
- **db_error_logs**: Captures execution errors, SQL states, stack traces, and calling context parameters.
