import React from 'react';
import { Sparkles, LogOut, Building2, MapPin, Layers, UserCheck } from 'lucide-react';
import { useAuth } from '../../hooks/useAuth';

export const PreviewModeBanner: React.FC = () => {
  return null;
};

